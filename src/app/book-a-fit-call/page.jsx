import ContactClient from "../contact/ContactClient";

export const metadata = {
  title: "Book a Fit Call | FI Digital UAE",
  description: "Schedule a diagnostic call with our AI and Zoho experts to explore how we can automate your business operations.",
};

export default function BookAFitCallPage() {
  return <ContactClient />;
}
